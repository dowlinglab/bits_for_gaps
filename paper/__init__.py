"""Paper reproduction (repo-only -- see REFACTOR_PLAN.md §7.3, not shipped in the wheel).

Importable as ``paper.figures.<fig>`` in dev/CI via ``tests/conftest.py``'s ``sys.path``
insert of the repo root (mirrors the ``examples/`` mechanism from Phase 6).
"""
