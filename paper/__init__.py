"""Paper reproduction (repo-only, not shipped in the wheel).

Importable as ``paper.figures.<fig>`` in dev/CI via ``tests/conftest.py``'s ``sys.path``
insert of the repo root (mirrors the same mechanism for ``examples/``).
"""
